# ==========================================
# CO-LAB CONNECT - Backend Environment Configuration Example
# Copy this file to .env and fill in your actual credentials.
# DO NOT commit your actual .env file to Git!
# ==========================================

# Core Application Security
# Use >= 32 bytes for SECRET_KEY / JWT_SECRET_KEY (HS256 warns below 32).
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-flask-key-here
JWT_SECRET_KEY=your-super-secret-jwt-key-here
DEMO_PASSWORD=choose-a-local-only-demo-password
FLASK_ENV=development

# Browser origins allowed to call the API (comma-separated, or * for any).
# Localhost dev origins are appended automatically by app/__init__.py.
CORS_ORIGINS=*

# Database Connections
# Default local database fallback
DATABASE_URL=sqlite:///collabconnect.db
ALLOW_SQLITE_FALLBACK=true

# Production Supabase PostgreSQL Connection String
SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.YOUR-PROJECT-REF.supabase.co:5432/postgres

# Production Neon Database Connection String (optional fallback)
NEON_DATABASE_URL=

# AI Assistant (OpenCode Zen model, backend only - NEVER expose to frontend)
# Get a key at https://opencode.ai/auth ; leave blank to use built-in rules.
OPENCODE_ZEN_API_KEY=
OPENCODE_ZEN_MODEL=big-pickle
OPENCODE_ZEN_URL=https://opencode.ai/zen/v1/chat/completions

# External API Credentials (Backend Only - NEVER expose to frontend)
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_PUBLISHABLE_KEY=your_supabase_publishable_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_keep_private
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
NEON_API_KEY=your_neon_api_key_here

# Payment & Notification Settings
PAYMENT_MODE=sandbox
PAYMENT_GATEWAY_KEY=
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SECONDS=30

# OTP delivery: true returns the code in the API response and shows it in the
# UI (demo mode, no SMS gateway). Set to false once real mail is configured.
OTP_EXPOSE_IN_RESPONSE=true

# Money rates. MUST match app/services/pricing.py DEFAULT_* (the single source
# of truth). Changing these changes worker payouts and invoices.
# Commission is the ONLY deduction - there is no welfare or tax deduction.
COMMISSION_RATE=0.10
TAX_RATE=0.00
COMMISSION_INCLUDE_MATERIAL=false

# Optional SMTP (Resend etc.) for real OTP emails; see DEPLOY.md section 4
MAIL_SERVER=smtp.resend.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=noreply@yourdomain.com

# Gunicorn / process tuning (Render injects PORT). Free plan: keep workers at 1.
# WEB_CONCURRENCY=1
# GUNICORN_TIMEOUT=60
# LOG_LEVEL=info

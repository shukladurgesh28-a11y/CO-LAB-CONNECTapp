# =============================================================================
# MOVED — do not use this file.
# =============================================================================
# Render only reads a Blueprint from the REPOSITORY ROOT. It never looks inside
# subfolders, so this file was silently ignored while looking authoritative —
# and it carried a committed `postgresql://postgres:[FILL IN PASSWORD]@...`
# placeholder that would be applied verbatim if anyone wired it up by hand.
#
# The real, single source of truth is:
#
#     ->  render.yaml   (repository root)
#
# It defines both services (API + static-site frontend) and is the file
# referenced by DEPLOY.md section 2.
# =============================================================================
